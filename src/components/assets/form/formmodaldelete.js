import React from "react";

export default function FormModalDelete(){
    return  (
        <div className={
            "absolute " +
            "w-full h-full " +
            "bg-opacity-25 bg-background"
        }>

        </div>
    )
}